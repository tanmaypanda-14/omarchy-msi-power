# tanmay.msi-power — the ec_sys module must be loaded with write support so
# the raw EC register file (/sys/kernel/debug/ec/ec0/io) can be used for the
# USB power-share toggle. Without write_support the debugfs file stays 0400.
options ec_sys write_support=1