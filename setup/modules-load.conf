# tanmay.msi-power — preload the DKMS EC driver and the (read-only) raw EC
# interface at boot so the sysfs attributes and the fan tachometer are
# available without any daemon or MControlCenter running.
msi_ec
ec_sys