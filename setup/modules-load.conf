# tanmay.msi-power — preload the DKMS EC driver and the raw-EC interface at
# boot so the sysfs attributes and the USB power-share byte are available
# without any daemon or MControlCenter running.
msi_ec
ec_sys