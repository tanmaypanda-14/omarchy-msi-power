# tanmay.msi-power — preload ec_sys with write_support at boot (the option is
# set in /etc/modprobe.d/msi-ec.conf) so the USB power-share toggle works
# without any running daemon or MControlCenter.
ec_sys